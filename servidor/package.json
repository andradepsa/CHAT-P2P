{
  "name": "k10-signaling-server",
  "version": "1.0.0",
  "description": "Servidor de sinalização WebRTC para o K10 Chat",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "peer": "^1.0.2",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
